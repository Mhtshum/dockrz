version: '3.8'

services:
  react-app:
    build: .
    ports:
      - "5173:5173"
    volumes:
    - ./ca/src:/app/vt-app/src:ro   # Mount only the host src folder as read-only
    environment:
      - CHOKIDAR_USEPOLLING=true      # Required for file watching on Windows
